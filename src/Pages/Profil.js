import React from 'react';
import PropTypes from 'prop-types';

const Profil = props => {
    return (
        <>
            <p>Profil</p>
        </>
    );
};

Profil.propTypes = {
    
};

export default Profil;