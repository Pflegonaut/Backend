import React from 'react';
import PropTypes from 'prop-types';

const Kontrollieren = props => {
    return (
        <>
            <p>Kontrollieren</p>
        </>
    );
};

Kontrollieren.propTypes = {
    
};

export default Kontrollieren;