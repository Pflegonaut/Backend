import React from 'react';
import ReactLoading from 'react-loading';

const Loading = () => {
    return (
        <>
        <ReactLoading type="spin" color="#82d8cb" height={87} width={25} />
        </>
    );
};

export default Loading;